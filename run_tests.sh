#!/bin/bash
export ROOT_DIR=`pwd`
export SCRIPT_DIR="${ROOT_DIR}/src"
export DATA_DIR="${ROOT_DIR}/data"
export OUTPUT_ROOT="${ROOT_DIR}/output"

# Shari:  add commands to run the radar flood mapping Ike imagery

# See run_tests.sh in ../tornadoTrack
